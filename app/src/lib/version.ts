// Single source of truth for the app version shown in the UI.
// Keep in sync with the top entry of CHANGELOG.md (repo root) and
// app/package.json "version" (asserted by version.test.ts).
export const APP_VERSION = '1.5.0';
